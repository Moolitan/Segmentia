# Section 6.1: experimental setup

Edit the shared `../config.py` and this directory's `config.py`, then run:

```bash
bash run.sh
```

This step does not start vLLM. It records hardware, model/tokenizer paths,
workload token counts, and offline Catalog availability in JSON and CSV.
