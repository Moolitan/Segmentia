# Section 6.2: correction quality

This zero-argument experiment compares Full, Direct, fixed-prefix,
ratio-prefix, and native CacheBlend on twelve frozen public-Skill tasks. It
saves every response and per-rule judgment, plus mergeable `samples.csv` and
`summary.csv` files. Tools are not executed.

```bash
bash run.sh
```
