# Section 6.3: latency scaling

The runner measures server-side API-receive-to-first-token TTFT for four Skill
lengths and every enabled platform. It saves raw JSON, JSONL, mergeable sample
CSV, summary CSV, and one grouped bar chart per platform.

```bash
bash run.sh
```
